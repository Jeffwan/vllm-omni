# ---------------------------------------------------------------------------
# Step 1: StormService + MooncakeConnector + ZMQ (Multi-Node Omni-Modality)
# ---------------------------------------------------------------------------
#
# This manifest deploys a 3-stage omni-modal pipeline using AIBrix
# StormService with MooncakeConnector as the data plane and ZMQ (tcp://)
# for lightweight task notifications between stages.
#
#   Role "thinker"  (Stage 0) — Multimodal understanding + text generation
#   Role "talker"   (Stage 1) — Text embeddings → audio codec codes
#   Role "code2wav" (Stage 2) — Audio codec codes → audio waveform
#
# Communication:
#   - Data plane (heavy): MooncakeConnector (tensors, hidden states)
#   - Control plane (light): ZMQ PUSH/PULL tcp:// (task notifications)
#
# Each role runs on its own node. The existing _stage_worker() loop
# is unchanged — OmniQueue("tcp") is a drop-in for mp.Queue.
#
# Prerequisites:
#   1. AIBrix operator installed (v0.4.0+)
#   2. Mooncake master service running (see mooncake-master Deployment below)
#   3. Kubernetes cluster with GPU nodes
#   4. pip install mooncake-transfer-engine pyzmq (baked into container image)
#
# Deploy:
#   kubectl apply -f stormservice_qwen2_5_omni_mooncake.yaml
# ---------------------------------------------------------------------------

# ===========================================================================
# 1. Mooncake Master — centralized metadata + control plane
# ===========================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mooncake-master
  labels:
    app: mooncake-master
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mooncake-master
  template:
    metadata:
      labels:
        app: mooncake-master
    spec:
      containers:
        - name: mooncake-master
          image: kvcache-ai/mooncake:latest
          command: ["mooncake_master"]
          args:
            - "--rpc_port=50051"
            - "--enable_http_metadata_server=true"
            - "--http_metadata_server_host=0.0.0.0"
            - "--http_metadata_server_port=8080"
            - "--metrics_port=9003"
            - "--root_fs_dir=/data/mc_storage/"
            - "--cluster_id=omni-cluster-1"
          ports:
            - containerPort: 50051
              name: grpc
            - containerPort: 8080
              name: metadata
            - containerPort: 9003
              name: metrics
          volumeMounts:
            - name: mc-storage
              mountPath: /data/mc_storage
          resources:
            requests:
              cpu: "2"
              memory: "4Gi"
      volumes:
        - name: mc-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: mooncake-master
spec:
  selector:
    app: mooncake-master
  ports:
    - port: 50051
      targetPort: 50051
      name: grpc
    - port: 8080
      targetPort: 8080
      name: metadata
  type: ClusterIP

# ===========================================================================
# 2. StormService — 3-role omni-modality pipeline
# ===========================================================================
---
apiVersion: orchestration.aibrix.ai/v1alpha1
kind: StormService
metadata:
  name: qwen25-omni-mooncake
  labels:
    model.aibrix.ai/name: "Qwen2.5-Omni-7B"
    model.aibrix.ai/engine: "vllm-omni"
spec:
  # Pooled mode: each role scales independently
  replicas: 1
  stateful: true
  updateStrategy:
    type: InPlaceUpdate
  selector:
    matchLabels:
      app: qwen25-omni-mooncake
  template:
    metadata:
      labels:
        app: qwen25-omni-mooncake
        model.aibrix.ai/name: "Qwen2.5-Omni-7B"
    spec:
      # RoleSet update strategy: update roles one at a time
      # (thinker first, then talker, then code2wav)
      updateStrategy: Sequential

      roles:
        # ---------------------------------------------------------------
        # Role 0: Thinker
        # ---------------------------------------------------------------
        - name: thinker
          replicas: 1
          stateful: true
          upgradeOrder: 0
          template:
            metadata:
              labels:
                model.aibrix.ai/port: "8000"
                model.aibrix.ai/engine: "vllm-omni"
                omni.vllm.ai/stage-id: "0"
                omni.vllm.ai/stage-role: "thinker"
            spec:
              # Anti-affinity: spread stages across nodes
              affinity:
                podAntiAffinity:
                  preferredDuringSchedulingIgnoredDuringExecution:
                    - weight: 100
                      podAffinityTerm:
                        labelSelector:
                          matchExpressions:
                            - key: app
                              operator: In
                              values: ["qwen25-omni-mooncake"]
                        topologyKey: kubernetes.io/hostname
              containers:
                - name: vllm-omni-thinker
                  image: vllm-omni/vllm-omni:latest
                  command: ["python", "-m", "vllm_omni.entrypoints.stage_service"]
                  args:
                    - "--model=Qwen/Qwen2.5-Omni-7B"
                    - "--stage-id=0"
                    - "--stage-type=llm"
                    - "--model-stage=thinker"
                    - "--model-arch=Qwen2_5OmniForConditionalGeneration"
                    - "--gpu-memory-utilization=0.8"
                    - "--enforce-eager"
                    - "--trust-remote-code"
                    - "--engine-output-type=latent"
                    - "--port=8000"
                    # Mooncake connector config
                    - "--connector-type=MooncakeConnector"
                    - "--connector-metadata-server=http://mooncake-master:8080/metadata"
                    - "--connector-master=mooncake-master:50051"
                    - "--connector-proto=tcp"
                  env:
                    - name: CUDA_VISIBLE_DEVICES
                      value: "0"
                    - name: VLLM_LOGGING_PREFIX
                      value: "[Stage-0-Thinker]"
                    # ZMQ queue endpoints (OmniQueue transport)
                    - name: OMNI_QUEUE_TRANSPORT
                      value: "tcp"
                    - name: OMNI_QUEUE_IN_ENDPOINT
                      value: "tcp://0.0.0.0:5560"   # bind: receives tasks
                    - name: OMNI_QUEUE_OUT_ENDPOINT
                      value: "tcp://0.0.0.0:5561"   # bind: sends results
                    # Next stage's in_q endpoint (for orchestrator forwarding)
                    - name: OMNI_NEXT_STAGE_ENDPOINT
                      value: "tcp://qwen25-omni-mooncake-talker-0:5560"
                    # Mooncake needs to know this pod's IP for registration
                    - name: POD_IP
                      valueFrom:
                        fieldRef:
                          fieldPath: status.podIP
                  ports:
                    - containerPort: 5560
                      name: zmq-in
                    - containerPort: 5561
                      name: zmq-out
                  resources:
                    limits:
                      nvidia.com/gpu: "1"
                    requests:
                      nvidia.com/gpu: "1"
                      cpu: "4"
                      memory: "32Gi"
                  readinessProbe:
                    tcpSocket:
                      port: 5560
                    initialDelaySeconds: 120
                    periodSeconds: 10
                  securityContext:
                    capabilities:
                      add: ["IPC_LOCK"]

        # ---------------------------------------------------------------
        # Role 1: Talker
        # ---------------------------------------------------------------
        - name: talker
          replicas: 1
          stateful: true
          upgradeOrder: 1
          template:
            metadata:
              labels:
                model.aibrix.ai/port: "8001"
                model.aibrix.ai/engine: "vllm-omni"
                omni.vllm.ai/stage-id: "1"
                omni.vllm.ai/stage-role: "talker"
            spec:
              affinity:
                podAntiAffinity:
                  preferredDuringSchedulingIgnoredDuringExecution:
                    - weight: 100
                      podAffinityTerm:
                        labelSelector:
                          matchExpressions:
                            - key: app
                              operator: In
                              values: ["qwen25-omni-mooncake"]
                        topologyKey: kubernetes.io/hostname
              containers:
                - name: vllm-omni-talker
                  image: vllm-omni/vllm-omni:latest
                  command: ["python", "-m", "vllm_omni.entrypoints.stage_service"]
                  args:
                    - "--model=Qwen/Qwen2.5-Omni-7B"
                    - "--stage-id=1"
                    - "--stage-type=llm"
                    - "--model-stage=talker"
                    - "--model-arch=Qwen2_5OmniForConditionalGeneration"
                    - "--gpu-memory-utilization=0.8"
                    - "--enforce-eager"
                    - "--trust-remote-code"
                    - "--engine-output-type=latent"
                    - "--engine-input-source=0"
                    - "--port=8001"
                    - "--connector-type=MooncakeConnector"
                    - "--connector-metadata-server=http://mooncake-master:8080/metadata"
                    - "--connector-master=mooncake-master:50051"
                    - "--connector-proto=tcp"
                  env:
                    - name: CUDA_VISIBLE_DEVICES
                      value: "0"
                    - name: VLLM_LOGGING_PREFIX
                      value: "[Stage-1-Talker]"
                    - name: OMNI_QUEUE_TRANSPORT
                      value: "tcp"
                    - name: OMNI_QUEUE_IN_ENDPOINT
                      value: "tcp://0.0.0.0:5560"
                    - name: OMNI_QUEUE_OUT_ENDPOINT
                      value: "tcp://0.0.0.0:5561"
                    - name: OMNI_NEXT_STAGE_ENDPOINT
                      value: "tcp://qwen25-omni-mooncake-code2wav-0:5560"
                    - name: POD_IP
                      valueFrom:
                        fieldRef:
                          fieldPath: status.podIP
                  ports:
                    - containerPort: 5560
                      name: zmq-in
                    - containerPort: 5561
                      name: zmq-out
                  resources:
                    limits:
                      nvidia.com/gpu: "1"
                    requests:
                      nvidia.com/gpu: "1"
                      cpu: "4"
                      memory: "32Gi"
                  readinessProbe:
                    tcpSocket:
                      port: 5560
                    initialDelaySeconds: 120
                    periodSeconds: 10
                  securityContext:
                    capabilities:
                      add: ["IPC_LOCK"]

        # ---------------------------------------------------------------
        # Role 2: Code2Wav
        # ---------------------------------------------------------------
        - name: code2wav
          replicas: 1
          stateful: true
          upgradeOrder: 2
          template:
            metadata:
              labels:
                model.aibrix.ai/port: "8002"
                model.aibrix.ai/engine: "vllm-omni"
                omni.vllm.ai/stage-id: "2"
                omni.vllm.ai/stage-role: "code2wav"
            spec:
              affinity:
                podAntiAffinity:
                  preferredDuringSchedulingIgnoredDuringExecution:
                    - weight: 100
                      podAffinityTerm:
                        labelSelector:
                          matchExpressions:
                            - key: app
                              operator: In
                              values: ["qwen25-omni-mooncake"]
                        topologyKey: kubernetes.io/hostname
              containers:
                - name: vllm-omni-code2wav
                  image: vllm-omni/vllm-omni:latest
                  command: ["python", "-m", "vllm_omni.entrypoints.stage_service"]
                  args:
                    - "--model=Qwen/Qwen2.5-Omni-7B"
                    - "--stage-id=2"
                    - "--stage-type=llm"
                    - "--model-stage=code2wav"
                    - "--model-arch=Qwen2_5OmniForConditionalGeneration"
                    - "--gpu-memory-utilization=0.3"
                    - "--enforce-eager"
                    - "--trust-remote-code"
                    - "--engine-output-type=audio"
                    - "--engine-input-source=1"
                    - "--port=8002"
                    - "--connector-type=MooncakeConnector"
                    - "--connector-metadata-server=http://mooncake-master:8080/metadata"
                    - "--connector-master=mooncake-master:50051"
                    - "--connector-proto=tcp"
                  env:
                    - name: CUDA_VISIBLE_DEVICES
                      value: "0"
                    - name: VLLM_LOGGING_PREFIX
                      value: "[Stage-2-Code2Wav]"
                    - name: OMNI_QUEUE_TRANSPORT
                      value: "tcp"
                    - name: OMNI_QUEUE_IN_ENDPOINT
                      value: "tcp://0.0.0.0:5560"
                    - name: OMNI_QUEUE_OUT_ENDPOINT
                      value: "tcp://0.0.0.0:5561"
                    # Final stage: no next stage endpoint
                    - name: POD_IP
                      valueFrom:
                        fieldRef:
                          fieldPath: status.podIP
                  ports:
                    - containerPort: 5560
                      name: zmq-in
                    - containerPort: 5561
                      name: zmq-out
                  resources:
                    limits:
                      nvidia.com/gpu: "1"
                    requests:
                      nvidia.com/gpu: "1"
                      cpu: "4"
                      memory: "16Gi"
                  readinessProbe:
                    tcpSocket:
                      port: 5560
                    initialDelaySeconds: 120
                    periodSeconds: 10
                  securityContext:
                    capabilities:
                      add: ["IPC_LOCK"]

# ===========================================================================
# 3. Headless Services for ZMQ pod-to-pod discovery
# ===========================================================================
# StormService stateful pods get stable DNS names:
#   qwen25-omni-mooncake-thinker-0.thinker-headless.default.svc.cluster.local
#   qwen25-omni-mooncake-talker-0.talker-headless.default.svc.cluster.local
#   qwen25-omni-mooncake-code2wav-0.code2wav-headless.default.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: thinker-headless
spec:
  clusterIP: None
  selector:
    omni.vllm.ai/stage-role: thinker
  ports:
    - port: 5560
      targetPort: 5560
      name: zmq-in
    - port: 5561
      targetPort: 5561
      name: zmq-out
---
apiVersion: v1
kind: Service
metadata:
  name: talker-headless
spec:
  clusterIP: None
  selector:
    omni.vllm.ai/stage-role: talker
  ports:
    - port: 5560
      targetPort: 5560
      name: zmq-in
    - port: 5561
      targetPort: 5561
      name: zmq-out
---
apiVersion: v1
kind: Service
metadata:
  name: code2wav-headless
spec:
  clusterIP: None
  selector:
    omni.vllm.ai/stage-role: code2wav
  ports:
    - port: 5560
      targetPort: 5560
      name: zmq-in
    - port: 5561
      targetPort: 5561
      name: zmq-out
